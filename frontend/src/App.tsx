import {useEffect,useState} from 'react';import {BrowserRouter,Route,Routes} from 'react-router-dom';import type {Theme} from './types';import {AppLayout} from './components/layout/AppLayout';import {ChatPage} from './pages/ChatPage';import {DocumentsPage} from './pages/DocumentsPage';import {SearchPage} from './pages/SearchPage';import {SettingsPage} from './pages/SettingsPage';import {HistoryPage} from './pages/HistoryPage';
export default function App(){const [collapsed,setCollapsed]=useState(localStorage.getItem('cb-sidebar')==='collapsed');const [theme,setThemeState]=useState<Theme>((localStorage.getItem('cb-theme') as Theme)||'system');const setTheme=(value:Theme)=>{setThemeState(value);localStorage.setItem('cb-theme',value)};useEffect(()=>{const resolved=theme==='system'?(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'):theme;document.documentElement.dataset.theme=resolved},[theme]);return <BrowserRouter><Routes><Route element={<AppLayout collapsed={collapsed} onToggle={()=>setCollapsed(v=>{localStorage.setItem('cb-sidebar',!v?'collapsed':'expanded');return !v})}/> }><Route path="/" element={<ChatPage/>}/><Route path="/documents" element={<DocumentsPage/>}/><Route path="/search" element={<SearchPage/>}/><Route path="/history" element={<HistoryPage/>}/><Route path="/settings" element={<SettingsPage theme={theme} setTheme={setTheme}/>}/></Route></Routes></BrowserRouter>}
