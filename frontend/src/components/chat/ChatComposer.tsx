import {Send} from 'lucide-react';import {useState} from 'react';
export function ChatComposer({onSend,busy}:{onSend:(text:string)=>void;busy:boolean}){const [text,setText]=useState('');return <form className="composer" onSubmit={e=>{e.preventDefault();if(text.trim()&&!busy){onSend(text.trim());setText('')}}}><textarea aria-label="Ask Corporate Brain" value={text} onChange={e=>setText(e.target.value)} placeholder="Ask Corporate Brain anything..." onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();e.currentTarget.form?.requestSubmit()}}}/><button disabled={busy||!text.trim()} aria-label="Send"><Send size={18}/></button></form>}
