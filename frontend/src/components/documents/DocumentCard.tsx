import {Trash2} from 'lucide-react';import type {DocumentItem} from '../../types';
export function DocumentCard({item,onDelete}:{item:DocumentItem;onDelete:(id:string)=>void}){return <article className="document-card"><span className="badge">{item.type.toUpperCase()}</span><div><strong>{item.name}</strong><small>{item.blocks} prepared blocks · {item.status.replaceAll('_',' ')}</small></div><button className="danger" onClick={()=>onDelete(item.id)} aria-label={`Delete ${item.name}`}><Trash2 size={16}/></button></article>}
