import {RefreshCw,Trash2} from 'lucide-react';import type {DocumentItem} from '../../types';
export function DocumentCard({item,onDelete,onReindex}:{item:DocumentItem;onDelete:(id:string)=>void;onReindex:(id:string)=>void}){return <article className="document-card glass-card"><span className="badge">{item.type.toUpperCase()}</span><div><strong>{item.name}</strong><small>{item.blocks} prepared blocks · {item.status.replaceAll('_',' ')}</small></div><button onClick={()=>onReindex(item.id)} aria-label={`Re-index ${item.name}`} title="Re-index only this document"><RefreshCw size={16}/></button><button className="danger" onClick={()=>onDelete(item.id)} aria-label={`Delete ${item.name}`}><Trash2 size={16}/></button></article>}
